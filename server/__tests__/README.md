# Server Tests

> These tests were inherited from the original CAP repo and cover the Express/Nunjucks server in `server/`.
>
> They are not relevant to the UR prototype, which is a static site in `docs/`.

## Running

```bash
npm test
```
