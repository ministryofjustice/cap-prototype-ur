# Deployment

The prototype is deployed automatically to **GitHub Pages** from the `docs/` folder on the `main` branch.

No manual deployment steps are needed. Merge to `main` and GitHub Pages will update within a few minutes.

---

> The infrastructure runbooks below were inherited from the original CAP repo and do not apply to this UR prototype.
