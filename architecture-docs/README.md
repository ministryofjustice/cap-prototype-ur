# Architecture

This is a static HTML/CSS/JS prototype deployed to GitHub Pages. There is no server-side architecture.

All state is managed client-side using `localStorage` via `docs/assets/js/state.js`.

---

> The ADRs and diagrams below were inherited from the original CAP repo and do not apply to this UR prototype.
